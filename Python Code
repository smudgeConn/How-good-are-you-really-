def better_than_average(class_points, your_points):

    # input: list, int
    # output: boolean
    
    num = sum(class_points) + your_points
    
    average = num/(len(class_points) + 1)
    
    if your_points > average:
        return True
    else:
        return False
